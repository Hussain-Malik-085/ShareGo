# Env (use .env.example; copy to .env and add keys)
.env
.env.local
.env.*.local

node_modules/
.expo/
*.log
.DS_Store

# iOS – CocoaPods (regenerate with: cd ios && pod install)
ios/Pods/
ios/build/
ios/*.xcworkspace/xcuserdata/
ios/.xcode.env.local

# Android – local / generated (Metro/Gradle builds; do not commit)
android/.gradle/
android/build/
android/app/build/
android/.idea/
android/local.properties
android/*.iml
android/.cxx/
android/app/.cxx/
